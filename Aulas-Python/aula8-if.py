# Create to program that asks to that
# the user report the value of your note
# Case the note be greaten than or  equal
# the 7.0, so it will show "approved"

average = int(input("\nEnter your note: ")) 

if average >= 7:

 print("\nApproved =)\n")

if average < 7 and average >= 4:

 print("\nYou need to do a exam =/\n")
 
if average < 4:
  
 print("\nReproved =(\n")